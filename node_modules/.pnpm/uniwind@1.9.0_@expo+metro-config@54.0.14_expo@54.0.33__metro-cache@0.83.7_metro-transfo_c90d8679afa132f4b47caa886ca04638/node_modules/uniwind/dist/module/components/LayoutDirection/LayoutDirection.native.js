import { jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { View } from "react-native";
import { UniwindContext, useUniwindContext } from "../../core/context.js";
export const LayoutDirection = ({ rtl, children }) => {
  const uniwindContext = useUniwindContext();
  const value = useMemo(
    () => rtl === void 0 ? uniwindContext : { ...uniwindContext, rtl },
    [uniwindContext, rtl]
  );
  const style = useMemo(() => {
    if (rtl === void 0) {
      return {
        display: "contents"
      };
    }
    return { display: "contents", direction: rtl ? "rtl" : "ltr" };
  }, [rtl]);
  return /* @__PURE__ */ jsx(View, { style, children: /* @__PURE__ */ jsx(UniwindContext.Provider, { value, children }) });
};
