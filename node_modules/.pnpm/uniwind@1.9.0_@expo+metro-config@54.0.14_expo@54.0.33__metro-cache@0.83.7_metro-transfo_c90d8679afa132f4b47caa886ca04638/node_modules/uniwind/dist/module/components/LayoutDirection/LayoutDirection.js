import { jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { UniwindContext, useUniwindContext } from "../../core/context.js";
export const LayoutDirection = ({ rtl, children }) => {
  const uniwindContext = useUniwindContext();
  const value = useMemo(
    () => rtl === void 0 ? uniwindContext : { ...uniwindContext, rtl },
    [uniwindContext, rtl]
  );
  const dir = rtl === void 0 ? void 0 : rtl ? "rtl" : "ltr";
  return /* @__PURE__ */ jsx("div", { dir, style: { display: "contents" }, children: /* @__PURE__ */ jsx(UniwindContext.Provider, { value, children }) });
};
